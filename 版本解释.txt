V1: 1. 先旋转图像，后使用椎体内边界中心以及椎体前缘顶点作为中心线的确定依据；
      2. 使用与运算确定直线与轮廓的交点；
      3. 使用blur均值滤波对二值化的图像进行滤波。

V2: 1. 改用中值滤波对二值化图像进行滤波操作；
      2. 取消图像旋转；
      3. 采用椎体内边界中心以及椎体内边界底角作为中心线的确定依据；
      4. 二值化的阈值改为160。 

V3: 1. 增加了两个参数的计算。

V4: 1. 通过像素点与直线的距离来确定直线与像素点的交点，点与直线距离小于1时，确定为交点；
      2. 皮肤提取的二值化阈值改为20；
      
V5: 1. 采用椎体内边界底角以及椎体外边界底角作为中心线的确定依据；
      2. 

V6: 1. 进一步加大ROI区域的范围；
      2. 增加了如果内轮廓边界是闭合状态的参数提取过程；
      3. 使用轮廓面积作为轮廓筛选条件；
      4. 使用椎体内边界“中心”以及椎体外轮廓底角点构建直线AE；

V7: 1. 增加了针对多种内轮廓草图进行的内轮廓提取方法；
      2. 增加从右侧（L）提取参数的功能。

V8: 1. 使用椎体外轮廓的内接圆圆心以及椎体内轮廓底角点作为椎体中心线确定依据。

V9: 1. 使用内轮廓内切圆圆心作为椎体内轮廓中心点；
      2. 将算法中涉及到的交点均改为使用最小距离来确定；
      3. 添加旋转算法，在参数提取前对图像进行旋转摆正。